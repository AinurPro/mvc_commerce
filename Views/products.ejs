<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <h1>products Page</h1>

    
    <ul>
        <% data.map( (product, index) => { %>
    <li>
        <%= product.title %> is
        <%= product.price %> and is
        <%= product.description %>
    
    
    </li>
        <%} ) %>
    </ul>
</body>
</html>